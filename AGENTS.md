# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# UI/UX & Motion Engineering (Emil Kowalski Skills)

For all UI/UX styling, motion, and interaction design in this app, strictly adhere to the skills codified in `.agents/skills/`:

- `.agents/skills/animate-expo`: Follow the build sequence, thread-safety rules (never `setState` in gestures/scroll, use Reanimated 4, keep on UI runtime), and recipes in `RECIPES.md` (press feedback, sheets, headers, transitions).
- `.agents/skills/emil-design-eng`: Follow high-craft design engineering principles (instant press feedback, intentional easing, avoiding generic AI patterns).
- `.agents/skills/review-animations` & `.agents/skills/improve-animations`: Audit motion code and adhere to the strict craft bar.
